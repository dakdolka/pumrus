# Backend database (legacy runtime; migration to PostgreSQL is a separate step)
host=127.0.0.1
user=change-me
password=change-me
db=pumrus_db
port=3306

# Telegram
TOKEN=replace-with-bot-token
WEB_APP_URL=https://example.com
API_URL=http://backend:8000/api
TG_PROXY=

# Use "polling" only for local development.
# Production must use "webhook".
BOT_MODE=polling
WEBHOOK_BASE_URL=https://example.com
WEBHOOK_PATH=/telegram/webhook
WEBHOOK_SECRET=replace-with-a-long-random-secret
WEBHOOK_PORT=8080
